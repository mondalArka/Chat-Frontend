{optionSelected === "Reply" && participants.length > 0 ?
            (
                <div
                    onMouseLeave={() => setOptionSelected("")}
                    style={{
                        background: "rgba(30, 34, 53, 0.97)",
                        border: "1px solid #2d3748",
                        borderRadius: "10px",
                        padding: "4px",
                        minWidth: "140px",
                        maxHeight: "200px",
                        overflowY: "auto",
                    }}
                >
                    {participants.map(p => (
                        <button
                            key={p.id}
                            onClick={() => setReplyTo(p.id as unknown as string)}
                            style={{
                                display: "block",
                                width: "100%",
                                background: "none",
                                border: "none",
                                cursor: "pointer",
                                color: "#e2e8f0",
                                fontSize: "13px",
                                fontWeight: 400,
                                padding: "6px 10px",
                                borderRadius: "7px",
                                textAlign: "left",
                                whiteSpace: "nowrap",
                                transition: "background 0.15s",
                            }}
                            onMouseEnter={e => (e.currentTarget.style.background = "#2d3748")}
                            onMouseLeave={e => (e.currentTarget.style.background = "none")}
                        >
                            {p.name}
                        </button>
                    ))}
                </div>
            ) : null
        }